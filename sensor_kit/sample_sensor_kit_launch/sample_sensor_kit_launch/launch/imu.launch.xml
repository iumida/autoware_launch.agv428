<launch>
  <arg name="launch_driver" default="true"/>
  <arg name="vehicle_id" default="default"/>

  <group>
    <push-ros-namespace namespace="imu"/>

    <!-- 加入 WIT IMU Driver 節點 -->
    <node pkg="wit_ros2_imu"
          exec="wit_ros2_imu"
          name="imu"
          output="screen">
      <!-- remap imu/data_raw → /imu_raw -->
      <remap from="imu/data_raw" to="imu_raw"/>
      <!-- 參數設定 -->
      <param name="port" value="/dev/imu_usb"/>
      <param name="baud" value="921600"/>
    </node>

    <node pkg="xsens_mti_ros2_driver"
          name="xsens_mti_node"
          exec="xsens_mti_node"
          output="screen"
          if="$(var launch_driver)">
      <remap from="/imu/data" to="imu_raw2"/>
      <param name="config_file"
             value="$(find-pkg-share xsens_mti_ros2_driver)/param/xsens_mti_node.yaml"/>
      <param name="time_option" value="2"/>
      <param name="frame_id"    value="imu_link"/>
      <param name="pub_transform"      value="false"/>
      <param name="enable_deviceConfig" value="true"/>
    </node>

    <!-- 改為使用 remapped 之 imu_raw 為輸入 -->
    <arg name="imu_raw_name" default="imu_raw"/>
    <arg name="imu_corrector_param_file"
         default="$(find-pkg-share individual_params)/config/$(var vehicle_id)/sample_sensor_kit/imu_corrector.param.yaml"/>

    <!-- IMU 校正 -->
    <include file="$(find-pkg-share autoware_imu_corrector)/launch/imu_corrector.launch.xml">
      <arg name="input_topic"  value="$(var imu_raw_name)"/>
      <arg name="output_topic" value="imu_data"/>
      <arg name="param_file"   value="$(var imu_corrector_param_file)"/>
    </include>

    <!-- 偏移估測 -->
    <include file="$(find-pkg-share autoware_imu_corrector)/launch/gyro_bias_estimator.launch.xml">
      <arg name="input_imu_raw"            value="$(var imu_raw_name)"/>
      <arg name="input_odom"               value="/localization/kinematic_state"/>
      <arg name="imu_corrector_param_file" value="$(var imu_corrector_param_file)"/>
    </include>
  </group>
</launch>
